services:
  backend:
    build:
      context: .
      dockerfile: infra/docker/backend.Dockerfile
    environment:
      PYTHONPATH: /workspace/shared/python
      CYBERBOX_AUTO_SEED: "true"
    volumes:
      - ./logs/observability:/workspace/logs/observability
      - ./data/evaluation_ground_truth:/workspace/data/evaluation_ground_truth
      - ./apps/frontend/test-results/red-agent:/workspace/apps/frontend/test-results/red-agent
    ports:
      - "8000:8000"

  frontend:
    build:
      context: .
      dockerfile: infra/docker/frontend.Dockerfile
    environment:
      VITE_API_BASE_URL: http://localhost:8000
    ports:
      - "5173:5173"
    depends_on:
      - backend
